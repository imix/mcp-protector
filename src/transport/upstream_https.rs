//! Upstream HTTPS transport.
//!
//! Connects to a remote MCP server over TLS using rustls (no OpenSSL).
//! Attaches the configured bearer token to every outbound request (FR14–FR15).
//! Loads the platform's native certificate store so that corporate CAs are
//! trusted without extra configuration (Decision 9).
//!
//! Stub — full implementation in Story 1.7.

// Stub module: items will be populated in subsequent stories.
